import { Header } from "./Header";

export default function CustomHTML() {
    return (
        <html lang="en">
            <body>
                    <Header />
            </body>
        </html>
    );
}